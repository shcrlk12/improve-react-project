// Replace your-framework with the name of your framework
import type { Meta } from "@storybook/react";
import DailyOperatingTable from "@components/preview/table/DailyOperatingTable";

const meta: Meta<typeof DailyOperatingTable> = {
  component: DailyOperatingTable,
};

export default meta;
